<!DOCTYPE html>
<html lang="en" data-theme="system">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Two-Factor Authentication - Nebulae</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="{{ url_for('static', filename='css/style.css') }}">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="flex flex-col min-h-screen items-center justify-center py-8 px-4 bg-cover bg-center bg-no-repeat bg-fixed backdrop-blur-sm" style="background-image: url('{{ url_for('static', filename='images/branding/background_1920x1080.png') }}');">
    <!-- Dark overlay for better readability -->
    <div class="fixed inset-0 bg-black/30 -z-10"></div>
    
    <div class="max-w-md w-full bg-white dark:bg-gray-800 p-8 rounded-lg shadow-xl border border-gray-200 dark:border-gray-700">
        <h1 class="text-2xl font-bold mb-6 text-center primary-text">Two-Factor Authentication</h1>
        
        {% with messages = get_flashed_messages(with_categories=true) %}
            {% if messages %}
                {% for category, message in messages %}
                    <div class="mb-4 p-3 rounded {% if category == 'danger' %}bg-red-100 text-red-700{% else %}bg-green-100 text-green-700{% endif %}">
                        {{ message }}
                    </div>
                {% endfor %}
            {% endif %}
        {% endwith %}
        
        <form method="POST" action="{{ url_for('auth.login') }}">
            <input type="hidden" name="username" value="{{ username }}">
            <input type="hidden" name="password" value="">
            
            <p class="mb-4 secondary-text">Enter your authentication code:</p>
            
            <div class="mb-4">
                <input type="text" 
                       name="otp_code" 
                       id="otp_code"
                       maxlength="8"
                       required
                       autofocus
                       class="w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 form-input text-center text-2xl tracking-widest"
                       placeholder="000000">
                <p class="text-xs secondary-text mt-2 text-center">
                    6-digit code from your app OR 8-character backup code
                </p>
            </div>
            
            <button type="submit" 
                    class="w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 px-4 rounded-lg">
                Verify
            </button>
            
            <div class="mt-6 border-t border-gray-200 dark:border-gray-700 pt-4">
                <details class="mb-3">
                    <summary class="cursor-pointer text-sm font-medium primary-text hover:text-blue-600">
                        Lost access to your authenticator app?
                    </summary>
                    <div class="mt-2 text-xs secondary-text space-y-2 pl-4">
                        <p><strong>Use a backup code:</strong></p>
                        <p>Enter one of your 8-character backup codes (like "A3F2B8C1") in the field above instead of a 6-digit code.</p>
                        <p class="text-yellow-600 dark:text-yellow-400">⚠️ Each backup code can only be used once.</p>
                        <p class="mt-3"><strong>Don't have your backup codes?</strong></p>
                        <p>You'll need to contact an administrator to disable 2FA on your account.</p>
                    </div>
                </details>
            </div>
            
            <div class="mt-4 text-center">
                <a href="{{ url_for('auth.login') }}" class="text-sm text-blue-600 hover:underline">
                    Back to login
                </a>
            </div>
        </form>
    </div>
    
    <script src="{{ url_for('static', filename='js/app.js') }}"></script>
    <script>
        // Auto-format input as user types
        document.getElementById('otp_code').addEventListener('input', function(e) {
            // Remove any spaces or dashes
            this.value = this.value.replace(/[\s-]/g, '').toUpperCase();
        });
    </script>
</body>
</html>
